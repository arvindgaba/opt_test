# --- core maths / data ---
numpy>=2.3,<2.4
pandas>=2.3,<2.4          # needs the matching NumPy range

# --- web app & charts ---
streamlit>=1.47,<2.0
streamlit-autorefresh>=1.0,<2.0
plotly>=7.0,<8.0

# --- market data & scraping ---
yfinance>=0.2,<0.3
tradingview-datafeed==2.1.1   # provides the tvDatafeed module you import
cloudscraper>=1.2.71

# --- networking / SSL (leave unpinned or pin if you want) ---
requests>=2.31
urllib3>=2.2
certifi>=2025.6
